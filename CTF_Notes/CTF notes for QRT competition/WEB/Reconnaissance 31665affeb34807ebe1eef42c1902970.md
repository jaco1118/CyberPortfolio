# Reconnaissance

- Check `.DS_Store` for Mac files.
- Check `robots.txt` for hidden paths.
- Check `server-status` for Apache info.
- Check `.htaccess` for server configs.
- Run `gobuster` for directory brute-forcing.